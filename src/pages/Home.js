import { TitleAndFilter } from '../components/TitleAndFilter';
import { CardDisplay } from '../components/CardsDisplay';

export const Home = () =>{
    return(
        <div>
        <TitleAndFilter/>
        <CardDisplay/>
        </div>
    );
};